#!/bin/sh

# short for (daniel reinstall crontab)
# drecron

currentDir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
mainCrontabFile="$currentDir/../"

echo "\$currentDir: $currentDir"
echo "\$currentDir: $currentDir"
echo "\$currentDir: $currentDir"
echo "\$currentDir: $currentDir"
  # source $currentDir/../profile

