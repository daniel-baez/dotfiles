#!/bin/sh

# loads every other file in this same directory
currentDir="$(dirname "${BASH_SOURCE}")";

for file in $currentDir/{aliases,editor,exports,history}.sh; do
	[ -r "$file" ] && [ -f "$file" ] && source "$file";
done;

unset file;
unset currentDir;


