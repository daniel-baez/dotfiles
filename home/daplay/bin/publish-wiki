#!/bin/sh

/usr/local/bin/nvim --headless -n -c VimwikiIndex \
  -c '3,$!sort | uniq' \
  -c 'w!' \
  -c VimwikiDiaryIndex \
  -c VimwikiDiaryGenerateLinks \
  -c 'w!' \
  -c VimwikiAll2HTML \
  -c 'qa!'

nvim="$!"
echo "nvim es: $nvim"
if [ ! -z $nvim ]; then
  kill -9 $nvim
fi
