#!/usr/bin/env bash
# Switch to the default branch (main or master) and pull.
set -euo pipefail

if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
  echo "not a git repository" >&2
  exit 1
fi

default=$(git default-branch)
echo "switching to $default"
git switch "$default"
git pull
