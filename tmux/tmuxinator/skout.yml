# ~/.tmuxinator/daplay.yml

name: daplay
root: ~/workspace/

windows:
  - editor:
      layout: even-vertical
      panes:
        - vim:
            - echo "hello daniel"
            - vim -c "call vimwiki#base#goto_index(2)"
